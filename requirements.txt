# Core - the app runs with just these.
numpy>=1.24
pypdf>=4.0
requests>=2.31
streamlit>=1.33

# Enables LLM-synthesized answers (used when ANTHROPIC_API_KEY is set in Secrets).
anthropic>=0.39

# Uncomment instead if you prefer OpenAI:
# openai>=1.40
